1. Extract the data set into a project file when using it
2. This program interpreter is Anaconda，You need to download and install the used package again
3. we use the Natural Language Toolkit (NLTK), a popular Python library for natural language processing. If you don't have NLTK installed, you can install   it using pip install nltk.

